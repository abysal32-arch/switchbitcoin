# Testnet4 dress rehearsal — LEG 1 evidence (task-21, started 2026-07-14)

Fund + onboard + first real signed-manifest ingest. Procedure:
`tasks/PRE-ALPHA-RUNBOOK.md` §2b; task spec `tasks/task-21-testnet-leg1/TASK.md`.

## 1. Preflight (2026-07-14)

- Node: bitcoind v29.4.0 `-testnet4 -txindex=1`, RPC 127.0.0.1:48332 (cookie).
  SYNCED at scan time: blocks = headers = **144126**, bestblock
  `00000000006f8561b4d20f9cfa0c4dcae098ac9b60307dc120aea285afcf7b1e`,
  `verificationprogress` 1, `initialblockdownload` false.
- CLI: `swapkey-cli 0.1.0 (git 4db91a755)` — matches repo HEAD `4db91a7`.
  Banner: `Swap Key PRE-ALPHA — REGTEST/TESTNET ONLY, NO REAL FUNDS`.
  Pinned manifest trust root shown:
  `fbb01df4f947cf69e8a24e4e907c60e8c903eb199e6dd949a2fabe5a5ea2191e`
  (the real operator pin — **no UNSHIPPABLE banner**).
- Pre-state `status`, both wallets: `coins: none`, `reserve: NONE`,
  `swaps: none`, manifest `v0 compiled baseline (PROVISIONAL — a
  fingerprintable anonymity partition…)` — expected round-1 staged state.

## 2. First REAL signed-manifest ingest (2026-07-14)

Command (both wallets):
`swapkey-cli manifest ingest docs/manifests/v1.manifest --config <cfg> --passphrase-stdin`

Result, identical on walletA and walletB:

```
[swapkey] manifest v1 ACCEPTED (was v0) — persisted; every future open runs it
manifest version: 1
id:               107b06657fc454ee199746401b1d358108d4fe82877145c0fae84827c6911a39
version floor:    1 (persisted; ingest requires a strictly higher version)
params:
  tier_d_sats:             1000000
  delta_fee_sats:          5000
  anchor_sats:             240
  setup_fee_sats:          1200
  cpfp_reserve_sats:       25000
  delta_early:             144
  margin:                  72
  delta_buffer:            24
  claim_confirm_allowance: 6
  cofunding_window:        12
  onboarding_delay_hours:  24..72
active posture:   Moderate
cofunding jitter: 6
quorum q:         3
```

Post-ingest verification: `manifest show` on both wallets → version 1,
floor 1, **provisional warning GONE**; `status` manifest line now
`v1 (id 107b0665…6911a39)`. The fleet is off the fingerprintable v0
partition; this is the Task-18 trust path proven against real wallet state
(pinned root → signed manifest → persisted floor).

## 3. Funding (JOE CHECKPOINT — faucet, human-gated)

`scantxoutset` at height 144126 confirmed both staged key-index-0 addresses
unfunded (`total_amount: 0.00000000`), so fresh faucet funding was requested.

Funding notes (real-faucet friction, recorded for the tester guide):
- testnet4.info pays **one payout per OAuth account** per cooldown; payout was
  ~0.0149 tBTC (decay model) — enough for 1 unit, not the 2-unit target.
  walletA was funded from Joe's first account; walletB required a second
  account.
- mempool.space's faucet refused a fresh free account ("Your account does not
  allow you to access the faucet").
- coinfaucet.eu (no account) paid only **0.00335416 tBTC** to walletB's
  key-index-0 address (`0d6a3e18ca2f4839a88a08ef389dc4c1bf760089b91f0cc550d9468a05982767:0`)
  — below the 0.0104 one-unit threshold; UTXOs do not combine, so this sits
  unonboarded (return to faucet at rehearsal end). walletB's real deposit went
  to a FRESH key-index-1 address instead.

| wallet | key index | deposit address | deposit txid:vout | amount (tBTC) | conf height |
|---|---|---|---|---|---|
| walletA | 0 | `tb1p96usly6kxv0q547paaukv7h7eskxyc8gg4lasp4tvv9fhjydupnq7mdz8s` | `2e172b54bda9ab9dfe42ed2e10af00a5e5595d4de70168e95b333314aa93d18d:1` | 0.01486585 | 144136 |
| walletB | 1 | `tb1pvh99yvphp2my8fggfe8ywu44wuxprrkhgjvv9npks5ktzmqd2shs3v9mqr` | `9393fc365eab2349458b81d953be14de4f2421786812b48560ab63d5f253a852:0` | 0.01485099 | 144136 |

Both deposits confirmed in the same block, 144136
(`0000000000e681b205a10c94ca18e384e30c42721ea4eed28f724846b8f765e0`,
time 1784077977).

## 4. Onboard + split (2026-07-14)

`swapkey-cli onboard <txid:vout> --key-index <n> --accept-phase0 --config <cfg>
--passphrase-stdin`. (Operational note: `onboard` shows the Phase-0 provenance
warning and blocks on a typed `ACCEPT` — non-interactive runs need
`--accept-phase0`.)

The two split transactions below are the FIRST live testnet transactions this
system ever broadcast:

| wallet | split txid | vsize | fee (sats) | outputs (sats) | conf height |
|---|---|---|---|---|---|
| walletA | `18480c6c97bdd8bce0a5a2456612036718fb39e891c3f65980392a9ee0afb173` | 197 | 2000 | unit 1005000 / change 454585 / reserve 25000 | 144138 |
| walletB | `b0d4081f101296e355e50091bfc9aa2453bf3793b0a0f4dd39b7c72abd1c5b2e` | 197 | 2000 | reserve 25000 / change 453099 / unit 1005000 | 144138 |

Both splits confirmed together in block 144138 (time 1784080379,
2026-07-15 01:52 UTC). Deposits were mined at 144136; the splits waited two
blocks (long testnet4 inter-block gaps, ~20 min each — NOT a fee problem, both
paid ~10.2 sat/vB, ~100× this node's 0.1 sat/vB relay floor).

Cross-checks: both splits vsize 197 vB, fee 2000 sats (~10.2 sat/vB, fallback
feerate source — expected: estimatesmartfee needs mempool-watch time on this
node). Output values exactly match the onboard plan (1 pre-encumbrance unit
1,005,000 + CPFP reserve 25,000 + change; deposit − outputs = 2,000 fee).
Output ORDER differs between the two splits (randomized placement working on
live txs).

Onboard resumed after split confirmation (the command re-run picks up the
pending split): both wallets logged `split confirmed at height 144138;
onboarding delays anchored`.

Post-onboard `status` (both wallets: manifest v1, `reserve: leasable (backstop
armed)`, 4 coins each):

```
walletA coins (4):
   1486585 sats  Deposit/Spent           2e172b54…d18d:1
   1005000 sats  PreEncumbrance/Unspent   18480c6c…b173:0
    454585 sats  OnboardingChange/Unspent 18480c6c…b173:1
     25000 sats  Reserve/Unspent          18480c6c…b173:2
walletB coins (4):
   1485099 sats  Deposit/Spent            9393fc36…a852:0
   1005000 sats  PreEncumbrance/Unspent   b0d4081f…5b2e:2
    453099 sats  OnboardingChange/Unspent b0d4081f…5b2e:1
     25000 sats  Reserve/Unspent          b0d4081f…5b2e:0
```

## 5. Maturity ETA

Onboarding delay per manifest v1: 24–72 h wall clock AND 144–432 testnet
blocks, anchored at split confirmation (`ledger.rs confirm_split`). The unit
becomes leasable when BOTH conditions clear (the later of the two).

Anchor: split block 144138, time 2026-07-15 01:52 UTC.

| bound | wall clock | block height |
|---|---|---|
| earliest leasable (+24 h / +144 blk) | 2026-07-16 01:52 UTC | 144282 |
| latest leasable (+72 h / +432 blk)   | 2026-07-18 01:52 UTC | 144570 |

Both wallets share the same anchor (both splits in block 144138), so their
maturity windows coincide. Leg 2 (task 25, the live swap) can run once
`status` shows the PreEncumbrance unit transition to leasable — earliest
~2026-07-16, and gated on testnet4 reaching height 144282+ (at the long block
gaps seen today, the height bound may lag the wall-clock bound). This ETA is
mirrored into `tasks/task-25-testnet-leg2-live-swap/TASK.md`.

## 6. Acceptance (task-21)

- [x] Both wallets: manifest **v1** active (provisional v0 partition warning
      gone) — §2.
- [x] ≥ 1 deposit onboarded each (A `2e172b54…d18d:1`, B `9393fc36…a852:0`) — §3.
- [x] Split confirmed (both in block 144138) — §4.
- [x] Delays anchored; `status` shows the pending unit + reserve carved,
      backstop armed — §4.
- [x] Evidence artifact committed (this file).
- [x] Maturity ETA recorded here + in task 25 — §5.

Round-2 note: only 1 unit funded per wallet (faucet per-account limits +
mempool.space refusal + coinfaucet dust — see §3). The 2-unit retry-capacity
target was not met; leg 2 has ONE swap attempt per side before needing more
matured units. Top-up (a second testnet4.info payout per account after
cooldown) can happen during the maturity wait to restore retry capacity.
